/** GENERAL */
.content {
  .title {
    @apply mb-3;
  }

  .categories-wrapper {
    @apply flex gap-2;
  }
  
  .description {
    @apply mb-3;

    img {
      @apply w-full rounded-lg;
      box-shadow: var(--material-shadow-md-1), var(--material-shadow-md-2);
    }

    a {
      @apply underline hover:no-underline underline-offset-1;
    }
  }

  /** CARD MODE */
  &.card-mode {
    .cover {
      @apply w-full h-60 object-cover object-top mb-2;
    }

    .title {
      @apply text-3xl font-semibold;
    }

    .categories-wrapper {
      @apply mb-3;
    }
  }

  /** SINGLE MODE */
  &.single-mode {
    .date, .title {
      @apply text-center;
    }

    .title {
      @apply text-5xl font-bold tracking-tight;
    }

    .subheader {
      @apply mb-5;

      @media (min-width: 1024px) {
        @apply flex justify-center items-center gap-5;
      }

      .categories-wrapper {
        @apply flex justify-center gap-2 mb-3;

        @media (min-width: 1024px) {
          @apply mb-0;
        }
      }
    }

    .cover {
      @apply max-w-full mx-auto mb-8;
    }

    .description {
      font-family: 'Rubik Variable', sans-serif;
      @apply text-xl font-normal;
    }
  }
}